[package]
name = "cavif"
description = "Encodes images in AVIF format (image2avif converter) using a pure-Rust encoder."
version = "1.8.1"
authors = ["Kornel Lesiński <kornel@geekhood.net>"]
edition = "2024"
license = "BSD-3-Clause"
readme = "README.md"
keywords = ["avif", "png2avif", "jpeg2avif", "convert", "av1"]
categories = ["command-line-utilities", "multimedia::images", "multimedia::encoding"]
homepage = "https://lib.rs/crates/cavif"
repository = "https://github.com/kornelski/cavif-rs"
include = ["README.md", "LICENSE", "/src/*.rs"]
rust-version = "1.90"

[dependencies]
clap = { version = "4.6.6", default-features = false, features = ["color", "suggestions", "wrap_help", "std", "cargo"] }
cocoa_image = { version = "1.1.0", optional = true }
imgref = "1.12.3"
load_image = "3.5.0-beta.1"
ravif = { version = "0.13", path = "./ravif", default-features = false, features = ["threading"] }
rayon = "1.12.0"
rgb = { version = "0.8.53", default-features = false }

[features]
default = ["asm"]
asm = ["ravif/asm"]
# obsolete
static = []
# moxcms by default
lcms2 = ["load_image/lcms2-static"]

[profile.dev]
opt-level = 1
debug = 1

[profile.release]
opt-level = 3
panic = "abort"
debug = false
lto = true
strip = true

[profile.dev.package."*"]
opt-level = 2

[dev-dependencies]
avif-parse = "2.1.0"

[badges]
maintenance = { status = "actively-developed" }

[workspace]
members = ["ravif"]

[package.metadata.docs.rs]
targets = ["x86_64-unknown-linux-gnu"]
rustdoc-args = ["--generate-link-to-definition"]
